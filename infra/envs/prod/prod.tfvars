# Prod overrides — to be completed in Delivery 2 once compute and storage
# decisions are finalized in Infraestructura en la Nube (E2).
#
# Expected changes vs dev:
#   environment           = "prod"
#   tickets_bucket_suffix = "<prod-specific-suffix>"
